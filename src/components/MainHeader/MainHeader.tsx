import React, { FC, useEffect } from 'react';
import './MainHeader.css';

export const MainHeader: FC = () => {
  return <header className="MainHeader">Mobx with classes for base store components</header>;
};
